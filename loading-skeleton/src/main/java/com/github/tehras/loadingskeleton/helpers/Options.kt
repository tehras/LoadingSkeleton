package com.github.tehras.loadingskeleton.helpers

/**
 * Options are passed from View Streamer to the individual converters
 */
data class Options(val gradient: Boolean, val shimmer: Boolean, val cornerRadius: Float, val color: Int)
